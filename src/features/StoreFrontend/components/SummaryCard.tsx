import styled from 'styled-components';
import { SurfaceCard } from '../../../components/styled';

export const SummaryCard = styled(SurfaceCard)`
  padding: 24px;

  @media (max-width: 860px) {
    padding: 18px;
  }
`;
